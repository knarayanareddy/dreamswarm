FROM rust:1.85-bookworm

RUN apt-get update && apt-get install -y \
    tmux \
    ripgrep \
    git \
    curl \
    jq \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
COPY . .
CMD ["cargo", "run"]
