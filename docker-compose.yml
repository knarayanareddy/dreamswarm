version: '3.8'

services:
  dreamswarm:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/workspace
      - cargo-cache:/usr/local/cargo/registry
    environment:
      - RUST_LOG=dreamswarm=debug
    stdin_open: true
    tty: true

volumes:
  cargo-cache:
